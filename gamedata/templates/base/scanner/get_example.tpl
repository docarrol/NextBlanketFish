<fetishmaster.engine.TextTemplate>
  <conditions>1;</conditions>
  <text>if ( proxy.isAdult() ) {&#xd;
   proxy = CharacterFromDNA( proxy.getDNA() );&#xd;
} else {&#xd;
   proxy = proxy;&#xd;
}</text>
  <priority>0</priority>
  <picturePath></picturePath>
  <choices/>
  <overrideChoices>false</overrideChoices>
</fetishmaster.engine.TextTemplate>