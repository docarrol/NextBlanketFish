<fetishmaster.engine.TextTemplate>
  <conditions>GetTextFlag(&quot;todd_current_proxy&quot;) != cuid;</conditions>
  <text>- Uhm, this is a different body than the one I use for the tests. Either switch back to the other proxy or reset the scan results entirely.</text>
  <priority>0</priority>
  <picturePath></picturePath>
  <choices/>
  <overrideChoices>false</overrideChoices>
</fetishmaster.engine.TextTemplate>